# Copy to .env.local and fill in real values (never commit .env.local)

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_ADMIN_USER_ID=

# Server-only — Vercel serverless functions (api/*.ts). No VITE_ prefix: never
# bundled into client code, never exposed to the browser.
SUPABASE_SECRET_KEY=
